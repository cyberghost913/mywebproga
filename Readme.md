Для запуска необходимо клонировать этот репозиторий, запустить базу данных в контейнере, используя команду ```docker-compose up -d```, затем установить необходимые зависимости командой ```pip install -r requirements.txt```, далее, чтобы создать модели в базе данных и заполнить ее моковыми данными надо выполнить миграцию, для этого выполните команду ```alembic upgrade head``` (если не надо заполнять базу данных, а только лишь создать модели введите ```alembic upgrade 1```), после этого можно запускать сервис командой ```uvicorn app.main:app```. Желательно выполнять все вышеперечисленные действия в виртуальном окружении.

---
Добавлена аутентификация (подробнее в docs/auth): закрыты все ручки, поддерживается авторизация по логину и паролю, либо через GitHub. Функционал остается прежним, за исключением того, что создание пользователя происходит автоматически при регистрации (упразднен метод POST по пути /users, добавление информации о пользователе происходит посредством метода PUT по пути /users/{user_id})

---
Добавлено кэширование (подробнее в docs/redis): добавлен "теплый" кэш новостей (ttl = 5 min), информация о сессиях перенесена в кэш, информация о правах пользователя вынесена в кэш, сделано логирование обращений к кэшу.

---
Добавлены моковые уведомления по email с использованием celery (подробнее в docs/worker): сообщения пользователям о новой новости и еженедельный дайджест. Приложение запускается как обычно, для запуска воркера используйте команду ```python -m app.background.consumer```, а для запуска регулярных задач используйте либо команду ```celery -A app.background.tasks beat --loglevel=info``` либо файл `start_beat.sh`